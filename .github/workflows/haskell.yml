name: Haskell CI

on:
  push:
    branches: ["*"]
  pull_request:
    branches: ["*"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: haskell-actions/setup@v2.9.1
        with:
          ghc-version: "9.10.3"
          cabal-version: "3.14.2.0"
      - name: Cache cabal
        uses: actions/cache@v5
        with:
          path: |
            ~/.cabal/store
            dist-newstyle
          key: ${{ runner.os }}-cabal-${{ hashFiles('haskell-chess.cabal') }}
          restore-keys: ${{ runner.os }}-cabal-
      - run: cabal update
      - name: Build all declared components with O2
        run: |
          cabal build \
            lib:haskell-chess \
            exe:haskell-chess \
            exe:tiny-main2 \
            exe:create-tiny \
            exe:convert-nnue \
            exe:bench-magic \
            test:haskell-chess-test \
            exe:bench-search \
            exe:bench-core \
            exe:bench-elo \
            exe:bench-tactics \
            exe:bench-report \
            exe:bench-san \
            exe:micro-bench \
            exe:bench-eval \
            exe:bench-evasions \
            exe:bench-pseudo-legal-moves \
            --jobs=1 \
            --ghc-options="-O2 +RTS -M4G -RTS"
      - run: cabal test haskell-chess-test --jobs=1 --ghc-options="-O2 +RTS -M4G -RTS"
